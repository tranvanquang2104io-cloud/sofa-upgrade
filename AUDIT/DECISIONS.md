# AUDIT — DECISIONS LOG

> Mọi quyết định tự chủ (chế độ qua đêm) được ghi ở đây kèm lý do. Chọn phương án an toàn/bảo tồn nhất khi mập mờ.

| # | Ngày | Bối cảnh | Quyết định | Lý do |
|---|------|----------|-----------|-------|
| D1 | 2026-07-25 | Chủ dự án duyệt 3 câu hỏi mở | (1) Số hiệu chứng từ → unique theo `(company_id, number)` qua migration rollback được; (2) `advance_skipped`: GIỮ nguyên logic dòng tiền, chỉ ghi luật suy luận, không đổi ý nghĩa; (3) Postgres cho prod, SQLite cho test, migration phải tương thích Postgres | Theo phê duyệt chính thức của chủ dự án |
| D2 | 2026-07-25 | Harness test cần DB nhưng không có Postgres/Docker | Dùng SQLite **file tạm theo từng test** (không dùng `:memory:`) | `:memory:` cấp DB riêng cho mỗi connection → Flask-SQLAlchemy mở nhiều connection sẽ thấy DB rỗng/false-fail. File tạm chia sẻ 1 DB, sạch từng test. |
| D3 | 2026-07-25 | pytest chưa cài | Cài `pytest` vào venv + tạo `requirements-dev.txt` | Hạ tầng test là lưới an toàn được phép dựng; không phải dịch vụ trả phí |
| D4 | 2026-07-25 | Harness lỗi trên SQLite: `postgresql.UUID` gọi `value.hex` khi nhận str (login truyền `session['user_id']` dạng str) | Thay `postgresql.UUID(as_uuid=True)` bằng `GUID` TypeDecorator (`app/models/types.py`): PostgreSQL vẫn ra `uuid` native (DDL & dữ liệu KHÔNG đổi), backend khác lưu CHAR(36) | Cần để chạy test trên SQLite; an toàn cho prod (Postgres không đổi schema). **Cần verify smoke trên Postgres thật trước khi merge** — xem FINAL-REPORT |
| D5 | 2026-07-26 | W6: D1 muốn unique theo `(company_id, number)` nhưng 4 bảng chứng từ chỉ có `order_id`, KHÔNG có `company_id`. Thêm cột company_id + backfill trên prod cần DB thật + backup để kiểm chứng — không làm an toàn/verify được đêm nay. | **Chọn:** DB-level unique theo `(order_id, number)` (không cần cột mới, migration reversible, verify được ngay) + **enforce per-company ở tầng app** (check trùng số qua join `Order.company_id`). Ràng buộc DB-level per-company chặt hơn (thêm cột `company_id`) → **đưa vào NEEDS-REVIEW (NR3)** để làm sau khi có prod DB. | An toàn & verify được đêm nay; vẫn sửa đúng bug B3 (tenant khác dùng lại số được). Khác biệt duy nhất: cùng công ty tái dùng số ở 2 order khác nhau — app vẫn chặn per-company. |
| D6 | 2026-07-26 | NR1 — Contract có bắt buộc quotation đã duyệt? (được chủ dự án ủy quyền quyết theo chuẩn ngành) | **KHÔNG bắt buộc (by design).** Chuẩn quote-to-cash/CPQ (Salesforce CPQ, Odoo, MS Dynamics) xem báo giá là khuyến nghị, không bắt buộc — cho phép tạo hợp đồng trực tiếp. | Giữ linh hoạt cho khách vãng lai/chốt miệng; B6 đóng là "by design". Không đổi code. |
| D7 | 2026-07-26 | NR2 — Cascade hard-delete vs soft-delete Company/Order | **Soft-delete là chuẩn.** Xác minh: không có route nào hard-delete Company/Order (chỉ dùng `is_active=False`/cancel). Giữ nguyên; không expose hard-delete; cascade DDL chỉ là lưới cuối. | Hồ sơ tài chính/pháp lý cần giữ để audit & tuân thủ thuế (thường 5–10 năm). Không rủi ro thêm vì không có đường xóa cứng. |
| D8 | 2026-07-26 | NR3 — Unique số hiệu per-company ở tầng DB (chủ dự án duyệt: "cứ làm qua migration rollback") | **Làm:** thêm `company_id` (GUID, FK companies) vào quotations/contracts/handover_records/payment_reports, backfill từ order, đổi unique `(order_id, number)` → `(company_id, number)`. | Đúng luật nghiệp vụ (số hiệu duy nhất trong 1 công ty); dữ liệu hiện globally-unique nên backfill không xung đột; cột company_id cũng hữu ích cho scope & query. |
| D9 | 2026-07-26 | Feature cột mở rộng (extend01–extend10) — chọn mô hình dữ liệu | **Reserved flex-columns** (`extend01..extend10 TEXT`) trên 4 bảng chứng từ + 1 bảng config `extension_field_configs` (per company, per entity_type, per slot: enabled/label/data_type/required/sort_order). Lưu text, validate/cast theo config ở tầng app. | Mẫu "flexfield" chuẩn (Oracle EBS flexfields, SAP Z-fields, ERPNext/Odoo custom fields). Không cần EAV phức tạp; admin bật/tắt + đặt nhãn + kiểu dữ liệu; cách ly tenant qua config per-company. |
