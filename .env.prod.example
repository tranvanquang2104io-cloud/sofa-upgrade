# Sao chép file này thành .env.prod và điền giá trị thật
# KHÔNG bao giờ commit .env.prod lên git

SECRET_KEY=<sinh bằng: python -c "import os; print(os.urandom(32).hex())">
POSTGRES_DB=sofa_flow
POSTGRES_USER=sofa_user
POSTGRES_PASSWORD=sofa_password
APP_VERSION=1.0.0
